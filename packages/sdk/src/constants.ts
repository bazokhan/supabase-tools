/**
 * Shared constants for Docker Compose file names.
 * Reduces duplication of hardcoded strings across packages.
 */
export const COMPOSE_DB_FILE = "docker-compose.db.yml";
export const COMPOSE_API_DOCS_FILE = "docker-compose.api-docs.yml";
